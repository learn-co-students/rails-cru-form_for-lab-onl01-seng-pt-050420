<%= form_for(@artist) do |f| %>
    <label> Artist name: </label>
    <!-- <%= f.label  :name %> -->
    <%= f.text_field :name %><br>
  
    <label> Artist bio: </label>
    <!-- <%= label_tag :bio %> -->
    <%= f.text_field :bio %><br>
  
    <%= f.submit %>
  <% end %>